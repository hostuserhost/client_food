import 'package:flutter/material.dart';

class TopBalancePage extends StatelessWidget {
  const TopBalancePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      '1000',
      style: TextStyle(
        color: Colors.black,
        fontSize: 20,
        fontFamily: 'Montserrat',
        fontWeight: FontWeight.normal,
        height: 0,
      ),
    );
  }
}
